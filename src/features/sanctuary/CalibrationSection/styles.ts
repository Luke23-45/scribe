import styled from 'styled-components';
import { motion } from 'framer-motion';

export const CalibrationContainer = styled(motion.div)`
  display: flex;
  gap: 24px;
  justify-content: center;
  margin-bottom: 64px;
  flex-wrap: wrap;
`;
